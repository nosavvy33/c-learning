#include <stdio.h>
                #ifndef MESSAGE
                        #define MESSAGE "Hello from the header section\n"
                #endif

                 int main(){ 
                printf(MESSAGE);
                return 0;
                }

